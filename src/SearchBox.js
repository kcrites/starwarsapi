import React from 'react';

const SearchBox = (searchchange) => {
	return (
		<div className='pa2'> 
		<button className='pa3 ba b--blue' type='button' onClick={searchchange}>Another</button>
		</div>
		)
}

export default SearchBox;